(* funzione data *)let rec intervals (min : float, max : float) (nsteps : int) : (float * float) list = []

let rec gauss_integral (interval : (float * float)) (nsteps : int) : float = 0.

let rec split_eq (l : int list) : int list list = []
